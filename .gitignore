# dependencies
node_modules/

# build output
dist/
dist-ssr/

# local env files — never commit these
.env
.env.*
!.env.example

# deployment
.vercel

# audio generation scratch (the script also uses node_modules/.cache)
*.aiff

# Pre-rendered word clips — untracked for now while the app runs on the
# browser's speech engine (USE_AUDIO_FILES is false in src/lib/audio.ts).
# Regenerate locally with: node scripts/generate-audio.mjs
public/audio/

# editors
.vscode/
.idea/
*.swp

# macOS
.DS_Store
**/.DS_Store

# logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# misc
*.local
coverage/
